import React from 'react';
import './buttons.css';

const LoginButton = () => {
    return (
        <button className="login-button">
            Login
        </button>
    );
};

export default LoginButton;